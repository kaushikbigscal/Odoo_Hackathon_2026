<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_assetflow_maintenance_request_list" model="ir.ui.view">
        <field name="name">assetflow.maintenance.request.list</field>
        <field name="model">assetflow.maintenance.request</field>
        <field name="arch" type="xml">
            <list string="Maintenance Requests">
                <field name="name"/>
                <field name="asset_id"/>
                <field name="requester_id"/>
                <field name="priority" widget="badge"/>
                <field name="technician_id"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_assetflow_maintenance_request_form" model="ir.ui.view">
        <field name="name">assetflow.maintenance.request.form</field>
        <field name="model">assetflow.maintenance.request</field>
        <field name="arch" type="xml">
            <form string="Maintenance Request">
                <header>
                    <button name="action_approve" type="object" string="Approve" class="btn-primary"
                            invisible="state != 'pending'"/>
                    <button name="action_reject" type="object" string="Reject" class="btn-danger"
                            invisible="state != 'pending'"/>
                    <button name="action_assign_technician" type="object" string="Assign Technician"
                            invisible="state != 'approved'"/>
                    <button name="action_start" type="object" string="Start Work" class="btn-primary"
                            invisible="state != 'technician_assigned'"/>
                    <button name="action_resolve" type="object" string="Resolve" class="btn-success"
                            invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="pending,approved,in_progress,resolved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="asset_id"/>
                            <field name="requester_id"/>
                            <field name="priority"/>
                        </group>
                        <group>
                            <field name="technician_id"/>
                            <field name="photo"/>
                        </group>
                    </group>
                    <field name="description" placeholder="Describe the issue..."/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_assetflow_maintenance_request" model="ir.actions.act_window">
        <field name="name">Maintenance Requests</field>
        <field name="res_model">assetflow.maintenance.request</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
